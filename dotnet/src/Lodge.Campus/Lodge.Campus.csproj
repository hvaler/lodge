<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <!-- Converse API. No reasoningConfig: extended thinking quadruples latency and is off by
         default through the API anyway (ADR-010). -->
    <PackageReference Include="AWSSDK.BedrockRuntime" Version="4.0.101.7" />
    <!-- The official MCP C# SDK. 2.2.0 is stable: this project's version policy allows no previews. -->
    <PackageReference Include="ModelContextProtocol" Version="2.2.0" />
  </ItemGroup>

</Project>
